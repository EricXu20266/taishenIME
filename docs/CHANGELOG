# CHANGELOG

## [Unreleased]

### Added
- 选词上屏（0.1.7）：
  - CTsfComposition：TSF 组合管理（StartComposition 创建 / UpdateComposition 更新 / CommitComposition 提交）
  - ITfCompositionSink::OnCompositionTerminated（应用撤销时清理状态）
  - 输入拼音时目标应用实时显示组合串；选词（数字/空格）替换为汉字并结束组合
  - 退格/ESC/光标键处理；编辑会话（TF_ES_SYNC+READWRITE）调度组合操作
  - 打通完整链路：按键→拼音→候选窗口→选词上屏
- Direct2D 候选窗口渲染（0.1.6）：
  - CCandidateWindow：Direct2D + DirectWrite 渲染（深色圆角背景、拼音串、候选词水平排布、选中高亮）
  - 置顶透明无边框窗口（WS_EX_TOPMOST/TOOLWINDOW/NOACTIVATE/LAYERED）
  - 光标定位：编辑会话（ITfEditSession）→ GetSelection → ITfContextView::GetTextExt
  - 内容自适应尺寸 + 屏幕超界回缩 + 候选空时自动隐藏 + ESC 取消
  - 冒烟测试：独立 exe 验证窗口创建/渲染/定位/隐藏，像素级确认渲染成功
- TSF 平台层实现（0.1.2 + 0.1.5）：
  - 正式 CLSID + 语言配置文件 GUID（替换占位符）
  - CTextService：ITfTextInputProcessorEx + ITfKeyEventSink + ITfThreadMgrEventSink
  - DLL 导出（.def 文件）：DllGetClassObject / DllCanUnloadNow / DllRegisterServer / DllUnregisterServer
  - CClassFactory 类工厂 + 模块引用计数
  - KeyEvent 处理（tsf_keyevent.cpp）：字母→engine_process_key、退格→engine_backspace、数字/空格→选词暂存
  - 注册表注册（HKCU）：CTF\TIP + LanguageProfile(zh-CN) + Category + CLSID InprocServer32
  - CMake：MSVC /utf-8 编译选项 + Rust std 系统库链接（ws2_32/ntdll/bcrypt/userenv）
  - 验证：DLL 构建成功、4 个 COM 入口导出、regsvr32 注册成功、COM 类工厂可实例化
- 项目初始化：Git 仓库、Biome 配置、.gitignore
- Rust 核心引擎骨架：
  - Engine 状态机（按键处理、候选管理、退格、重置）
  - 拼音模块（400+ 音节表、前缀校验、音节切分）
  - 词库模块（85 条内置词条，前缀索引 HashMap）
  - C FFI 接口（9 个 extern "C" 函数）
  - 10 个单元测试全通过
- Windows TSF 平台层骨架：
  - CMake 构建项目
  - DLL 入口 (dllmain.cpp)
  - TSF COM 组件骨架 (tsf_module.cpp)
  - 引擎桥接头文件 (engine_bridge.h)
- 文档体系：
  - ARCHITECT.md（拓扑分析 + Root 裁剪 + 结构映射）
  - business-flow.md（核心数据流）
  - DEV-TRACKER.md（需求看板）
  - index.md（文档索引入口）
