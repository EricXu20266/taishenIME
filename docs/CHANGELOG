# CHANGELOG

## [0.1.31] — 2026-08-03

### Added
- **V0.2.30 常用词分层 + 用户词热度学习**（选词优先级重排）：
  - **常用词层**：新增 `resources/common_dict.txt`（人工维护高频口语/书面词，行序即优先级），
    独立于 system_dict 加载，命中即出候选，根治同频生僻字压住常用字（如 `en`→「嗯」此前被「奀」压住）
  - **用户词热度学习**：用户词按"近期使用频率"分热/温两档——
    🔥 热词（7 天内累计 ≥3 次）压过常用词；🌡️ 温词（偶然打过）只在常用词后、系统词前插队；
    超 7 天自动降温，避免"打过一次永久占首位"误学污染
  - **排序升级**（query 五层）：精确：热词 > 常用词 > 温词 > 系统词；前缀层同构
  - 简拼（`hd`→好的）同步建常用词声母索引；双拼走 query 自动继承
  - 不参与 .bin 持久化（serde skip），用户改 common_dict.txt 下次启动生效，旧 .bin 兼容
- **第一屏验证与修复**：
  - 新增 check_first 工具枚举 409 音节前 5 候选（第一屏），两级检查：常用词前 5 可达（硬）+ 生僻字警告（软）
  - 修复 P2-2 长词过滤打乱 common 层：system 长词（如「爸爸」）不再硬塞进常用词排好的第一屏，
    common 词顺序用户显式指定，长词过滤跳过（目标词 + 插入位置均跳过）
  - 修复 5 个常用词被挤出第一屏（拔/伐/蹑/鸥/轻）；生僻字挤占从 13 处降至 7 处（余为音节本身常用字少）
  - FFI 集成测试升级：29 用例首位 + 229 常用词全部第一屏可达断言

## [0.1.30] — 2026-08-03

### Added
- **配置热加载**（对标 rime-ice 重新部署）：config.ini 修改后 2s 内自动生效，无需重启输入法。
  - 托盘消息窗口挂 2s 定时器轮询 config.ini mtime，变化即重载
  - 重载范围：候选数/模糊音/双拼/纠错/混输/简繁/主题（含跟随系统）/字体字号/行内预编辑/短语
  - 不重载词库（62 万条重建索引 1-2s，词库更新走 install 流程）
  - ActivateEx 配置应用段抽取为 ApplyConfig，Activate 与热加载共用

## [0.1.29] — 2026-08-03

### Added
- **V0.2.24 以词定字**：`[` 取当前候选首字，`]` 取末字上屏。
  生僻字/易错字场景——先打词再取字；取字不学习；无候选透传方括号
- **V0.2.25 拆字反查**：`u` + 部件拼音 → 生僻字候选。
  radical_pinyin.dict.yaml 13.2 万词条（rime-ice 拆字词库）；`u` 单独输入/双拼模式排除；
  引擎新增 radical.rs + engine_set_radical_path FFI
- **V0.2.26 错音错字提示**：候选不足时，易错读音映射表用正确读音补词。
  精选高频易错词（参差 cancha→cenci、暖和 nuanhe→nuanhuo、主角 zhujiao→zhujue、
  暴露 polu→baolu、标识 biaoshi→biaozhi 等 20+ 条）；补词不学习；
  拼音输入法不区分声调，只收声母/韵母拼写差异

## [0.1.28] — 2026-08-03

### Added
- **V0.2.23 英文自动大写**：输入 Hello → 候选 Hello，HELLO → 全大写。
  raw_input 保留大小写 + cap_state 判定（首字母大写/前 2 大写），退格实时重算
- **V0.2.22 计算器 cC 模式**：c + 算式 → 结果候选。
  四则 + - * /、括号、幂 ^、取模 %、小数；递归下降解析；除零/语法错无候选；
  一元 +/- 合法（1++2=3）；计算结果不学习用户词
- **V0.2.19 日期/时间/农历输入**：rq 日期 / sj 时间 / xq 星期 / nl 农历。
  日期 3 格式（ISO/中文/短）、时间 2 格式、星期 3 格式（中英）；
  农历 1900-2100 查表（闰月精确）；sj 与短语"手机"并存（日期优先）
- **V0.2.21 候选窗字体/字号可配**：config.ini `font_face` / `font_size`（12-32），
  DWrite 读取 + 行高/拼音行随字号缩放，DPI 缩放保留
- **V0.2.20 深色模式跟随系统**：注册表 AppsUseLightTheme 检测（Win10 1809+），
  候选窗 + 状态工具栏 WM_SETTINGCHANGE 实时切换；用户显式配置 theme_* 时固定不跟随
- **V0.2.18 行内预编辑**：`inline_preedit` 开关（默认开）——拼音写组合（光标处），
  候选窗不再重复画拼音行（布局/命中检测联动）；关闭回退候选窗显示拼音

## [0.1.27] — 2026-08-03

### Added
- **V0.2.17 符号输入 v 模式**（对标 rime-ice symbols.schema.yaml）：
  - `v` + 分类码触发符号候选：vjt 箭头 / vsx 数学 / vdw 单位 / vbd 标点
  - 首期 4 类高频符号（10-16 个/类），符号选中上屏不学习用户词
  - v 模式通过 pinyin_buf 前缀判定，无新状态字段；TSF 层零改动（复用字母/选词通道）
  - 双拼模式 v 是 zh 声母，自动排除；单独 v 按普通拼音处理
  - 新增 symbol.rs + 6 个单元测试（95 测试全过）
- **V0.2.16 词库扩容**（5.8 万 → 62 万条）：
  - 吸收 rime-ice ext.dict.yaml 全量（33.9 万，自带准确注音）
  - 吸收 tencent.dict.yaml 3 字词（22.4 万，pypinyin 批量自动注音）
  - 实测 3-5 字词 85 万全收内存过重 → 只收 3 字词（人名/地名/机构主力）
  - build_dict.py 下载走代理（raw.githubusercontent.com 直连 TLS 失败）

### Fixed
- **biome.json 配置错误**：`preset: "recommended"` 为非法键 → 改为 `recommended: true`，
  biome check 恢复可用
- **词库扩容后查询卡死**：单字母前缀（如 "l"）命中数万词条，query() 去重 O(n²) →
  前缀索引排序后截断到 top 100（候选最多 8 页×9=72 个，精确匹配词排截断前列，语义不变）

## [0.1.26] — 2026-08-02

### Added
- **V0.2.15 右下角状态横幅**（搜狗/QQ拼音同款）：
  - 切到泰深输入法 → 右下角常驻横幅（深色圆角 + 泰字 Logo + 品牌/状态两行）
  - 切走输入法（Deactivate）→ 自动隐藏
  - 中英/简繁/双拼切换 → 状态文字即时更新（如"中文模式 · 简繁 · 双拼"）
  - GDI 双缓冲绘制，置顶不抢焦点；新增 test_banner_window 冒烟测试
- **托盘菜单新增双拼/全拼切换**：原双拼只能改 config.ini，现托盘右键可切
- **混合简拼联想**（拼写运算）：shurf → 输入法（shu 全拼 + r/f 声母），
  zhongg → 中国；新增 full_index + query_mixed，82 测试全过

### Fixed
- **候选排序精确拼音优先**：system_dict 词组词频(3900+)碾压单字，输入
  wo/ni 候选全是词组不见单字 → 改为"精确拼音匹配优先 + 词频降序"，
  输入 wo 首位出"我"（women 仍在候选）
- **回车清空残留拼音**：拼音挂着按 Enter 时输入法透传但状态残留 →
  HandleKeyDown 加 VK_RETURN 分支先 engine_reset 再透传（微软拼音同款）
- **CMake 不追踪 Rust staticlib 依赖**：Rust 改动后增量构建不重链 DLL →
  记录到 commit，Rust 改动需 clean 重建

## [0.1.25] — 2026-08-02

### Fixed
- **切换输入法宿主崩溃（0xc000041d）**：`CandidateTheme` 构造函数死递归
  （`*this = Default()` 与 `Default(){ CandidateTheme t; }` 互相调用）→ 无限递归 →
  栈溢出。TSF CoCreateInstance 处于系统回调，栈溢出报 0xc000041d；
  独立 exe 报 0xc00000fd。影响 0.1.22~0.1.24（皮肤主题引入）。
  修复：默认构造初始化列表直接赋值，`Default()` 返回 `CandidateTheme()`。
- **build_vs18.bat 冒烟测试错误检查失效**：`if errorlevel 1` 对负退出码
  （崩溃码 0xC00000FD 有符号解释为负数）不成立 → 崩溃进程被误判为通过。
  改为 `if not "%errorlevel%"=="0"`。

## [0.1.24] — 2026-08-02

### Added
- **V0.2.14 多行候选面板（↓ 展开）**：
  - ↓ 展开多行网格（每行 5 个）/ ↑ 收起，Esc 处理透传
  - 多行布局：行数 = ceil(候选数/5)，窗口高度自适应，宽度 = 每行内容宽
  - 选中高亮跟随行列定位；翻页指示多行模式不显示
  - 鼠标点击选词支持多行行列命中（HitTest 加 y 坐标）
  - 按键接入：tsf_keyevent VK_DOWN/VK_UP → multirow_requested → tsf_module 应用
  - 冒烟测试：STEP6 验证 ↓ 展开请求 / ↑ 收起请求

## [0.1.23] — 2026-08-02

### Added
- **V0.2.13 状态栏/托盘图标**：
  - 系统托盘显示输入法状态图标：tooltip 实时显示"泰深输入法 - 中文/英文模式"
  - 左键单击：切换中英（与 Ctrl+Space 等效）
  - 右键菜单：切换中英 / 切换简繁 / 退出
  - 隐藏消息窗口作托盘宿主（WM_APP+1 自定义消息），ActivateEx 创建 / Deactivate 移除
  - 模式切换自动更新托盘 tooltip（UpdateTrayIcon 联动）
  - 务实版：系统图标 + tooltip 状态（D2D 文字图标列为后续增强）
  - 菜单文案动态显示当前状态（"切换到英文模式"/"关闭简繁转换"）

## [0.1.22] — 2026-08-02

### Added
- **V0.2.4 皮肤/主题系统**：
  - 候选窗口四色可配：背景/主文本/选中高亮/序号（theme_bg/theme_text/theme_highlight/theme_dim）
  - config.ini HEX 格式（RRGGBB，如 F5F5F5），非法值回退默认
  - 默认主题 = 深色（2E2E2E/E8E8E8/1E6FFF/9A9A9A，与 0.1.6 一致，零视觉变更）
  - `CandidateTheme` 结构：默认构造即深色，避免未配置时垃圾值
  - `CCandidateWindow::SetTheme`：运行时换肤（释放旧画刷重建，可见时立即重绘）
  - tsf_module ActivateEx 应用配置主题
  - 冒烟测试：默认主题/浅色主题解析/非法 HEX 回退

## [0.1.21] — 2026-08-02

### Added
- **V0.2.12 快捷短语**：
  - 简码 → 常用文本：输入 bq → 候选"不客气"，选中直接上屏
  - 内置短语表 20 条（bq/wm/dz/sj/gs/zj/wx/qq/email/tel/bz/hy/wd/bg/ht/fp/gsz/zgs/wns/xiex）
  - 自定义短语文件：`phrase_path` 指定（每行 code=text，# 注释，UTF-8），覆盖内置
  - 短语候选**排最前**（简码意图明确）；选中**不学习用户词**；不转繁体
  - config.ini 新增 `phrase` / `phrase_path` 配置项
  - FFI：`engine_set_phrase_enabled` / `engine_get_phrase_enabled` / `engine_set_phrase_path`
  - 平台层：tsf_module 应用 phrase 配置 + 加载短语文件
  - 测试：6 引擎单测（默认开/开关/建议/选中不学习/关闭/外部加载）+ FFI 集成（bq→不客气）

## [0.1.20] — 2026-08-02

### Added
- **V0.2.11 简繁转换**：
  - 内置简→繁映射：常用单字表（200+ 高频字）+ 词组表（100+，处理歧义如 头发→頭髮 vs 发现→發現）
  - 转换是**输出层**转换：词库/用户词库仍存简体，候选显示/上屏时转繁体
  - 用户词库学习存简体原词，输出再转（不污染用户词库）
  - 英文候选（混输）不转换
  - config.ini 新增 `traditional` 配置项（默认关）
  - FFI：`engine_set_traditional` / `engine_get_traditional`；`engine_get_candidate` 输出转换
  - 新模块 `engine/src/trad.rs`
  - 平台层：tsf_module 应用 traditional 配置
  - 测试：trad 模块 4 单测 + 引擎 5 测试（默认关/开关/输出转换/候选显示/英文不转）+ FFI 集成（中国→中國）

## [0.1.19] — 2026-08-02

### Added
- **V0.2.8 中英混输**：
  - 中文模式候选**末尾追加英文候选**（当前输入串原样），选中直接上屏英文，无需切换中英文
  - 场景：输入 hello 时候选末尾出现 hello，选它上屏，之后继续打中文
  - 选中英文候选**不学习用户词**（英文非拼音，避免污染用户词库）
  - 与 ascii_mode 区分：ascii_mode 是整会话英文直通，混输是中文模式下临时选英文
  - 条件：中文模式 + 全字母输入 + 非双拼（双拼码短歧义大不做）；ASCII 模式不追加
  - config.ini 新增 `mix_mode` 配置项（默认开）
  - FFI：`engine_set_mix_mode` / `engine_get_mix_mode`
  - 平台层：tsf_module 应用 mix_mode 配置
  - 测试：5 引擎单测（默认开/追加/选中不学习/关闭/ascii 模式）+ FFI 集成（hello 上屏 + 关闭验证）

## [0.1.18] — 2026-08-02

### Added
- **V0.2.10 智能纠错（错键纠正）**：
  - 键盘相邻键位容错：快速打字误触自动纠正（logn→long→龙、nihap→nihao→你好）
  - 两类变体：**相邻交换**优先（打字顺序错，n/g 换位）+ **单键替换**（误触相邻键，p/o）
  - QWERTY 相邻键映射（同行 + 上下行斜邻），惰性构建
  - 触发条件：精确候选不足时才纠错（不干扰正确输入），变体排精确/模糊之后
  - 支持模糊音叠加：纠错变体再走模糊音查询
  - 新模块 `engine/src/correction.rs`，30 个变体上限防膨胀
  - config.ini 新增 `correction` 配置项（默认开）
  - FFI：`engine_set_correction` / `engine_get_correction`
  - 平台层：tsf_module 应用 correction 配置
  - 测试：correction 模块 6 单测 + 引擎 4 测试 + FFI 集成（真实词库 logn→龙 翻页验证）

## [0.1.17] — 2026-08-02

### Added
- **V0.2.2 用户词库（学习 + 持久化）**：
  - 选词自动学习：`Engine::select_candidate` 命中即 learn（数字键/空格/鼠标选词统一走此路径）
  - 用户词独立 SQLite 存储（`%APPDATA%/taishen-ime/user_dict.db`），PRIMARY KEY(pinyin, word) 去重 + 词频累加
  - 查询插队：用户词按频率排系统词前（`dictionary::query` 合并 user_index），学过的词越用越靠前
  - 持久化：learn 即时写盘（INSERT ... ON CONFLICT DO UPDATE frequency+1），重启后保留
  - 容错：用户词库路径无效/写入失败 → 静默降级不阻塞输入；未启用时不学习
  - config.ini 新增 `user_dict_path` 配置项（空 = 默认 %APPDATA% 路径）
  - FFI：`engine_set_user_dict_path`（NULL = 禁用）
  - 平台层：tsf_module ActivateEx 读取配置后设置用户词库路径
  - 端到端测试：FFI 集成测试验证学习→持久化→重启置顶

## [0.1.16] — 2026-08-02

### Added
- **0.1.12 集成测试 + 性能基准（MVP 收尾）**：
  - `engine/tests/ffi_integration.rs`：走真实 C ABI 完整输入链路（初始化→按键→候选→选词上屏→退格→翻页→模式切换→错误码契约）
  - 性能基准（release）：160000 键 / 378.71ms → **单键 2.37µs，整串 18.94µs**（阈值 500µs，余量 200x）
  - `Cargo.toml` 增加 rlib crate-type（集成测试可链接）

### Fixed
- **TD-4 构建工具链彻底解决**：
  - 定位到 VS18 BuildTools **MSVC 14.51.36231**（`C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools`）
  - 移除 `/GS-` workaround（CMakeLists.txt）——14.51 与 Rust 1.97 lib 混链无 0xc0000409 崩溃
  - 移除 rust-lld 手工链接——14.51 自带 link.exe 直接链接成功
  - 冒烟测试（test_tsf_load / test_ascii_mode / test_config_reader）纳入 CMake 统一构建
  - 新增 `tools/build_vs18.bat` 一键构建脚本（configure → build → run tests）
  - 更新 test_ascii_mode 对齐 0.1.15 契约（英文模式字母透传而非 committed 提交）
  - 清理旧 ScopeCppSDK workaround 产物（build/ 目录、taishen_ime_lld.dll）

## [Unreleased]

### Added
- 吸收开源竞品功能（0.1.14，RIME/雾凇拼音）：
  - **词库扩容 14 倍**：吸收雾凇拼音（rime-ice）词库——8105 通用规范汉字全量 + base 基础词库高频 top 5 万 → 58658 条（8658 单字 + 5 万多字词），build_dict.py 自动下载转换（BSD 兼容）
  - **模糊音**（RIME Spelling Algebra 拼写变体）：平翘舌 z↔zh/c↔ch/s↔sh、前后鼻音 an↔ang/en↔eng/in↔ing、n↔l、f↔h、r↔l；查询时生成容错变体补入候选，默认开，config.ini 可关
  - **双拼方案**（RIME 微软双拼）：声母 zh→v/ch→i/sh→u + 韵母单键映射，零声母 'o' 开头；双拼码→全拼解码（含一键多韵母歧义），默认关，config.ini 可开
  - config.ini 新增 fuzzy / shuangpin 配置项（ParseBool 解析 1/true/on 等）
  - FFI：engine_set/get_fuzzy、engine_set/get_shuangpin
  - 44 引擎单元测试全过（新增模糊音变体/开关、双拼编解码/歧义/查询）
- 竞品功能补齐 + 关键 Bug 修复（0.1.13）：
  - **Bug 修复（实测根因）**：OnTestKeyDown 曾调用带副作用的 HandleKeyDown → 每键双处理（拼音双累积/退格双删/候选窗口不显示）。改为只读的 ShouldEatKey 预测试，处理只在 OnKeyDown 执行一次
  - **Bug 修复**：VK_BACK 无拼音时透传给应用（不再吞键），应用内退格删除恢复
  - 词库扩容：tools/build_dict.py（GB2312 一级 3755 单字 + 305 常用多字词 = 4060 条，pypinyin 注音）
  - 候选翻页：引擎 page 状态 + FFI（engine_page/current_page/total_pages）+ 翻页键（PgUp/PgDn、+/-、,/。）
  - 简拼支持：声母索引（query_short，zg→中国），zh/ch/sh 归一为 z/c/s
  - 多音节切分联想：phrase_guess（nihaoshijie→你好世界）
  - 鼠标点击选词：候选窗口 WM_LBUTTONUP 命中检测 + 回调（复用 TSF 组合提交链路）
  - 候选窗口翻页指示（"1/3"）+ 序号灰色视觉优化
  - 光标跟随健壮性：定位失败降级为鼠标位置（不再钉在屏幕左上角）
  - DPI 适配：字号/布局按 GetDpiForWindow 缩放
  - 24 引擎单元测试全过（新增翻页/简拼/切分/真实词库简拼验证）
- 安装器基础版（0.1.11）：
  - install.ps1：复制 DLL/词库 + regsvr32 注册（注册表校验）+ 生成 config.ini
  - uninstall.ps1：注销 + 删除安装目录（保留用户数据）
  - install.nsi：NSIS 正式安装器蓝图（待 NSIS 环境构建）
  - 端到端验证：install → 注册 → 卸载全链路通过
- FFI panic 守卫 + 日志（0.1.10）：
  - ffi_guard! 宏：catch_unwind 包裹全部 FFI 函数，panic 不跨 FFI 边界（防 UB 崩溃）
  - Mutex 锁中毒恢复（unwrap_or_else）
  - 轻量文件日志（%APPDATA%/taishen-ime/logs/engine.log）：初始化/词库/模式切换/销毁全记录
  - 17 单元测试全过（含锁中毒恢复 + 未初始化错误码测试）
- 中英文切换（0.1.9）：
  - 引擎 ascii_mode（默认中文）+ FFI engine_set/get_ascii_mode
  - Ctrl+Space 切换中英文（TSF 键盘事件捕获）
  - 英文模式字母直通上屏（复用 TSF 组合提交链路）
  - 冒烟测试：模式切换/英文直通/拼音累积全部通过
- 基础配置系统（0.1.8）：
  - config.ini 解析（key=value + # 注释，UTF-8），缺省回退默认值
  - candidate_count 配置：engine 侧 candidate_limit + FFI engine_set_candidate_count
  - dict_path 配置：相对 DLL 目录解析，空 = 内置词库
  - config_reader 独立测试 exe：解析/默认值/路径解析全部通过
  - 冒烟测试：12 单元测试全过 + DLL 构建 + 注册成功
- 选词上屏（0.1.7）：
  - CTsfComposition：TSF 组合管理（StartComposition 创建 / UpdateComposition 更新 / CommitComposition 提交）
  - ITfCompositionSink::OnCompositionTerminated（应用撤销时清理状态）
  - 输入拼音时目标应用实时显示组合串；选词（数字/空格）替换为汉字并结束组合
  - 退格/ESC/光标键处理；编辑会话（TF_ES_SYNC+READWRITE）调度组合操作
  - 打通完整链路：按键→拼音→候选窗口→选词上屏
- Direct2D 候选窗口渲染（0.1.6）：
  - CCandidateWindow：Direct2D + DirectWrite 渲染（深色圆角背景、拼音串、候选词水平排布、选中高亮）
  - 置顶透明无边框窗口（WS_EX_TOPMOST/TOOLWINDOW/NOACTIVATE/LAYERED）
  - 光标定位：编辑会话（ITfEditSession）→ GetSelection → ITfContextView::GetTextExt
  - 内容自适应尺寸 + 屏幕超界回缩 + 候选空时自动隐藏 + ESC 取消
  - 冒烟测试：独立 exe 验证窗口创建/渲染/定位/隐藏，像素级确认渲染成功
- TSF 平台层实现（0.1.2 + 0.1.5）：
  - 正式 CLSID + 语言配置文件 GUID（替换占位符）
  - CTextService：ITfTextInputProcessorEx + ITfKeyEventSink + ITfThreadMgrEventSink
  - DLL 导出（.def 文件）：DllGetClassObject / DllCanUnloadNow / DllRegisterServer / DllUnregisterServer
  - CClassFactory 类工厂 + 模块引用计数
  - KeyEvent 处理（tsf_keyevent.cpp）：字母→engine_process_key、退格→engine_backspace、数字/空格→选词暂存
  - 注册表注册（HKCU）：CTF\TIP + LanguageProfile(zh-CN) + Category + CLSID InprocServer32
  - CMake：MSVC /utf-8 编译选项 + Rust std 系统库链接（ws2_32/ntdll/bcrypt/userenv）
  - 验证：DLL 构建成功、4 个 COM 入口导出、regsvr32 注册成功、COM 类工厂可实例化
- 项目初始化：Git 仓库、Biome 配置、.gitignore
- Rust 核心引擎骨架：
  - Engine 状态机（按键处理、候选管理、退格、重置）
  - 拼音模块（400+ 音节表、前缀校验、音节切分）
  - 词库模块（85 条内置词条，前缀索引 HashMap）
  - C FFI 接口（9 个 extern "C" 函数）
  - 10 个单元测试全通过
- Windows TSF 平台层骨架：
  - CMake 构建项目
  - DLL 入口 (dllmain.cpp)
  - TSF COM 组件骨架 (tsf_module.cpp)
  - 引擎桥接头文件 (engine_bridge.h)
- 文档体系：
  - ARCHITECT.md（拓扑分析 + Root 裁剪 + 结构映射）
  - business-flow.md（核心数据流）
  - DEV-TRACKER.md（需求看板）
  - index.md（文档索引入口）
