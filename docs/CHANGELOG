# CHANGELOG

## [Unreleased]

### Added
- 吸收开源竞品功能（0.1.14，RIME/雾凇拼音）：
  - **词库扩容 14 倍**：吸收雾凇拼音（rime-ice）词库——8105 通用规范汉字全量 + base 基础词库高频 top 5 万 → 58658 条（8658 单字 + 5 万多字词），build_dict.py 自动下载转换（BSD 兼容）
  - **模糊音**（RIME Spelling Algebra 拼写变体）：平翘舌 z↔zh/c↔ch/s↔sh、前后鼻音 an↔ang/en↔eng/in↔ing、n↔l、f↔h、r↔l；查询时生成容错变体补入候选，默认开，config.ini 可关
  - **双拼方案**（RIME 微软双拼）：声母 zh→v/ch→i/sh→u + 韵母单键映射，零声母 'o' 开头；双拼码→全拼解码（含一键多韵母歧义），默认关，config.ini 可开
  - config.ini 新增 fuzzy / shuangpin 配置项（ParseBool 解析 1/true/on 等）
  - FFI：engine_set/get_fuzzy、engine_set/get_shuangpin
  - 44 引擎单元测试全过（新增模糊音变体/开关、双拼编解码/歧义/查询）
- 竞品功能补齐 + 关键 Bug 修复（0.1.13）：
  - **Bug 修复（实测根因）**：OnTestKeyDown 曾调用带副作用的 HandleKeyDown → 每键双处理（拼音双累积/退格双删/候选窗口不显示）。改为只读的 ShouldEatKey 预测试，处理只在 OnKeyDown 执行一次
  - **Bug 修复**：VK_BACK 无拼音时透传给应用（不再吞键），应用内退格删除恢复
  - 词库扩容：tools/build_dict.py（GB2312 一级 3755 单字 + 305 常用多字词 = 4060 条，pypinyin 注音）
  - 候选翻页：引擎 page 状态 + FFI（engine_page/current_page/total_pages）+ 翻页键（PgUp/PgDn、+/-、,/。）
  - 简拼支持：声母索引（query_short，zg→中国），zh/ch/sh 归一为 z/c/s
  - 多音节切分联想：phrase_guess（nihaoshijie→你好世界）
  - 鼠标点击选词：候选窗口 WM_LBUTTONUP 命中检测 + 回调（复用 TSF 组合提交链路）
  - 候选窗口翻页指示（"1/3"）+ 序号灰色视觉优化
  - 光标跟随健壮性：定位失败降级为鼠标位置（不再钉在屏幕左上角）
  - DPI 适配：字号/布局按 GetDpiForWindow 缩放
  - 24 引擎单元测试全过（新增翻页/简拼/切分/真实词库简拼验证）
- 安装器基础版（0.1.11）：
  - install.ps1：复制 DLL/词库 + regsvr32 注册（注册表校验）+ 生成 config.ini
  - uninstall.ps1：注销 + 删除安装目录（保留用户数据）
  - install.nsi：NSIS 正式安装器蓝图（待 NSIS 环境构建）
  - 端到端验证：install → 注册 → 卸载全链路通过
- FFI panic 守卫 + 日志（0.1.10）：
  - ffi_guard! 宏：catch_unwind 包裹全部 FFI 函数，panic 不跨 FFI 边界（防 UB 崩溃）
  - Mutex 锁中毒恢复（unwrap_or_else）
  - 轻量文件日志（%APPDATA%/taishen-ime/logs/engine.log）：初始化/词库/模式切换/销毁全记录
  - 17 单元测试全过（含锁中毒恢复 + 未初始化错误码测试）
- 中英文切换（0.1.9）：
  - 引擎 ascii_mode（默认中文）+ FFI engine_set/get_ascii_mode
  - Ctrl+Space 切换中英文（TSF 键盘事件捕获）
  - 英文模式字母直通上屏（复用 TSF 组合提交链路）
  - 冒烟测试：模式切换/英文直通/拼音累积全部通过
- 基础配置系统（0.1.8）：
  - config.ini 解析（key=value + # 注释，UTF-8），缺省回退默认值
  - candidate_count 配置：engine 侧 candidate_limit + FFI engine_set_candidate_count
  - dict_path 配置：相对 DLL 目录解析，空 = 内置词库
  - config_reader 独立测试 exe：解析/默认值/路径解析全部通过
  - 冒烟测试：12 单元测试全过 + DLL 构建 + 注册成功
- 选词上屏（0.1.7）：
  - CTsfComposition：TSF 组合管理（StartComposition 创建 / UpdateComposition 更新 / CommitComposition 提交）
  - ITfCompositionSink::OnCompositionTerminated（应用撤销时清理状态）
  - 输入拼音时目标应用实时显示组合串；选词（数字/空格）替换为汉字并结束组合
  - 退格/ESC/光标键处理；编辑会话（TF_ES_SYNC+READWRITE）调度组合操作
  - 打通完整链路：按键→拼音→候选窗口→选词上屏
- Direct2D 候选窗口渲染（0.1.6）：
  - CCandidateWindow：Direct2D + DirectWrite 渲染（深色圆角背景、拼音串、候选词水平排布、选中高亮）
  - 置顶透明无边框窗口（WS_EX_TOPMOST/TOOLWINDOW/NOACTIVATE/LAYERED）
  - 光标定位：编辑会话（ITfEditSession）→ GetSelection → ITfContextView::GetTextExt
  - 内容自适应尺寸 + 屏幕超界回缩 + 候选空时自动隐藏 + ESC 取消
  - 冒烟测试：独立 exe 验证窗口创建/渲染/定位/隐藏，像素级确认渲染成功
- TSF 平台层实现（0.1.2 + 0.1.5）：
  - 正式 CLSID + 语言配置文件 GUID（替换占位符）
  - CTextService：ITfTextInputProcessorEx + ITfKeyEventSink + ITfThreadMgrEventSink
  - DLL 导出（.def 文件）：DllGetClassObject / DllCanUnloadNow / DllRegisterServer / DllUnregisterServer
  - CClassFactory 类工厂 + 模块引用计数
  - KeyEvent 处理（tsf_keyevent.cpp）：字母→engine_process_key、退格→engine_backspace、数字/空格→选词暂存
  - 注册表注册（HKCU）：CTF\TIP + LanguageProfile(zh-CN) + Category + CLSID InprocServer32
  - CMake：MSVC /utf-8 编译选项 + Rust std 系统库链接（ws2_32/ntdll/bcrypt/userenv）
  - 验证：DLL 构建成功、4 个 COM 入口导出、regsvr32 注册成功、COM 类工厂可实例化
- 项目初始化：Git 仓库、Biome 配置、.gitignore
- Rust 核心引擎骨架：
  - Engine 状态机（按键处理、候选管理、退格、重置）
  - 拼音模块（400+ 音节表、前缀校验、音节切分）
  - 词库模块（85 条内置词条，前缀索引 HashMap）
  - C FFI 接口（9 个 extern "C" 函数）
  - 10 个单元测试全通过
- Windows TSF 平台层骨架：
  - CMake 构建项目
  - DLL 入口 (dllmain.cpp)
  - TSF COM 组件骨架 (tsf_module.cpp)
  - 引擎桥接头文件 (engine_bridge.h)
- 文档体系：
  - ARCHITECT.md（拓扑分析 + Root 裁剪 + 结构映射）
  - business-flow.md（核心数据流）
  - DEV-TRACKER.md（需求看板）
  - index.md（文档索引入口）
